% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spWAVE_class.r
\name{generate_kmeans_coord}
\alias{generate_kmeans_coord}
\alias{generate_kmeans_coord,spWAVE-method}
\alias{generate_kmeans_coord,data.frame-method}
\title{Generate Meta coordinates}
\usage{
generate_kmeans_coord(
  coord,
  centers = nrow(coord)/10,
  iter.max = 10,
  nstart = 1,
  random_seed = 42
)

\S4method{generate_kmeans_coord}{spWAVE}(
  coord,
  centers = nrow(coord)/10,
  iter.max = 10,
  nstart = 1,
  random_seed = 42
)

\S4method{generate_kmeans_coord}{data.frame}(
  coord,
  centers = nrow(coord)/10,
  iter.max = 10,
  nstart = 1,
  random_seed = 42
)
}
\arguments{
\item{coord}{object of class \code{data.frame}.}

\item{centers}{either the number of clusters, say \eqn{k}, or a set of
    initial (distinct) cluster centres.  If a number, a random set of
    (distinct) rows in \code{x} is chosen as the initial centres.}

\item{iter.max}{the maximum number of iterations allowed.}

\item{nstart}{if \code{centers} is a number, how many random sets
    should be chosen?}

\item{random_seed}{random seed of kmeans for reproducibility. Default is 42.}
}
\value{
If input is data.frame, return a list, meta coordinates contain geometric center of each cluster and
spot ids. And km_cluster contain the barcode and cluster id. If spWAVE, return spWAVE.
}
\description{
Generate meta coordinates by kmeans methods
}
\seealso{
\code{\link[stats]{kmeans}}
}
