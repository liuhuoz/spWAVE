% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{slice_tri_SpMat}
\alias{slice_tri_SpMat}
\title{Slice Triangular Sparse Matrix}
\usage{
slice_tri_SpMat(SpMat, row = NULL, col = NULL)
}
\arguments{
\item{SpMat}{triangular sparse matrix}

\item{row}{rows, can be rownames or indices. If null, return all rows.}

\item{col}{cols, can be colnames or indices. If null, return all cols.}
}
\value{
sliced dense sub-matrix.
}
\description{
Slice Triangular Sparse Matrix by given row and col
}
\details{
The goal of this function is to slice a large pairwise distance matrix
storing in sparse for saving memory and time.
}
\examples{
#Generate lower triangular sparse matrix
mat1 <- matrix(runif(25)*10,5,5)
rownames(mat1) <- paste0("row_",1:5)
colnames(mat1) <- paste0("col_",1:5)
tri_SpMat <- as(mat1*lower.tri(mat1),"dgCMatrix")
#sub matrix by index
sub_mat1 <- slice_tri_SpMat(tri_SpMat,row=1:3,col=2:4)
#sub matrix by character
sub_mat2 <- slice_tri_SpMat(tri_SpMat,row=paste0("row_",1:3),col=paste0("col_",2:4))
#sub matrix by mixed args
sub_mat3 <- slice_tri_SpMat(tri_SpMat,row=paste0("row_",1:3),col=2:4)
#sub_mat1,2,3 are definitely equal.
}
